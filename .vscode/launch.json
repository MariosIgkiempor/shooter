{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug game",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/shooter-debug.bin",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build game (debug)"
    }
  ]
}
