# Content expansion — build

Implementation tickets for [the content expansion spec](../../content-expansion/spec.md).

Numbered in dependency order, blockers first. The sibling `content-expansion/issues/`
directory holds the *wayfinder decision* tickets that produced the spec; those are all
resolved and are not build work.
